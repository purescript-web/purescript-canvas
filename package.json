{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "eslint src && spago build"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "purescript-psa": "^0.8.2",
    "rimraf": "^3.0.2",
    "spago": "^0.21.0"
  }
}
